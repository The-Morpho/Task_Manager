# Task_Manager
This is a simple task manager with 2 roles (admin and a simple user), the simple user can add tasks and his own only but the admin can add tasks and see all the tasks from all the users.
